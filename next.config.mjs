/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'images.unsplash.com', 'www.paypalobjects.com', 'firebasestorage.googleapis.com'],
    },
};

export default nextConfig;
